{
  "name": "multiverse",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "build:local": "./scripts/build-local.sh",
    "local:run": "node packages/local-runner/bin/agentis-local.js",
    "local:run:no-open": "node packages/local-runner/bin/agentis-local.js --no-open",
    "local:pack-run": "sh -c 'cd packages/local-runner && TARBALL=$(npm pack --silent) && npx \"./$TARBALL\" --no-open'",
    "local:pack-run:open": "sh -c 'cd packages/local-runner && TARBALL=$(npm pack --silent) && npx \"./$TARBALL\"'",
    "local:clean": "sh -c 'rm -rf packages/local-runner/bundle packages/local-runner/*.tgz apps/web/.next/standalone'"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@vitest/coverage-v8": "^4.0.18",
    "happy-dom": "^20.7.0",
    "turbo": "^2.8.14",
    "vitest": "^4.0.18"
  },
  "pnpm": {
    "overrides": {
      "@types/react": "^18.3.18",
      "@types/react-dom": "^18.3.5"
    }
  },
  "dependencies": {
    "fflate": "^0.8.2"
  }
}
